output {
  elasticsearch {
    hosts    => [ 'elasticsearch' ]
    index    => "%{[@metadata][beat]}-%{lc_custom_id}-%{+YYYY.MM.dd}"
    # document_id => "%{[@metadata][prefix]}%{[@metadata][fingerprint]}"
  }
}
